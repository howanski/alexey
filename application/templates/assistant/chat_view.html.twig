{% extends 'base.html.twig' %}
{% set breadcrumbs = [
	{'name': 'menu_record'|localised('assistant'), 'href': path('assistant_index')}
	]
%}

{% block javascripts %}
	{{ encore_entry_script_tags('forms-js') }}
	{{ encore_entry_script_tags('assistant-scripts') }}
	<script>
		window.addEventListener("load", (event) => {
	        window.scrollTo({
				top: document.body.scrollHeight,
				left: 0,
				behavior: "smooth",
        	});
		});
	</script>
{% endblock %}

{% block pageContentPrimary %}
	{% set action_buttons = [
		{
			'color': 'nord12',
			'href': path('assistant_index'),
			'name': 'menu_record'|localised('assistant'),
			'icon': 'fas fa-robot'
	   },
	] %}
	{% set action_buttons = action_buttons|merge(
		[
			{
				'color': 'nord10',
				'href': path('assistant_config'),
				'name': 'settings'|localised,
				'icon': 'fas fa-tools'
			},
		]
	) %}
	{% include 'entity_actions_row.html.twig' with {
		'buttons': action_buttons,
	} only %}
{% endblock %}

{% block pageContentSecondary %}
	{% set show_response_form = true %}
	<div class="flex flex-wrap">
		<div class="w-full px-4">
			<div class="relative flex flex-col min-w-0 break-words bg-nord4 w-full mb-6 shadow-lg rounded">
				<div class="rounded-t mb-0 px-4 py-3 border-0">
					<div class="flex flex-wrap items-center">
						<div class="relative w-full px-4 max-w-full flex-grow flex-1">
							<h3 class="font-semibold text-base text-nord0">
								<i class="fas fa-comments mr-1"></i>
								{{ 'chat'|localised('assistant') }}
							</h3>
						</div>
					</div>
				</div>
				<div class="p-4 space-y-6">
					{% for message_pair in chat.messages %}
						<!-- User message -->
						<div class="flex flex-col">
							<div class="flex items-start gap-3">
								<div class="flex-shrink-0">
									<div class="w-8 h-8 rounded-full bg-nord8 flex items-center justify-center text-nord0">
										<i class="fas fa-user text-sm"></i>
									</div>
								</div>
								<div class="flex-1 min-w-0">
									<div class="bg-nord2 text-nord4 rounded-lg rounded-tl-none px-4 py-3 text-sm whitespace-pre-wrap break-words overflow-y-auto">{{ message_pair.request|raw|trim }}</div>
								</div>
							</div>
						</div>

						<!-- LLM response -->
						<div class="flex flex-col">
							<div class="flex items-start gap-3">
								<div class="flex-shrink-0">
									<div class="w-8 h-8 rounded-full bg-nord8 flex items-center justify-center text-nord0">
										<i class="fas fa-robot text-sm"></i>
									</div>
								</div>
								<div class="flex-1 min-w-0">
									{% if message_pair.isProcessing %}
										<i class="fas fa-circle-notch animate-spin-5 refresh-page-on-check" data-check-path="{{ path('assistant_ajax_is_processed', {id: message_pair.id}) }}"></i>
										{% set show_response_form = false %}
									{% else %}
										<div class="bg-nord5 text-nord0 rounded-lg rounded-tl-none px-4 py-3 text-sm whitespace-pre-wrap break-words overflow-y-auto">{{ message_pair.response|raw|trim }}</div>
									{% endif %}
								</div>
							</div>
						</div>
					{% endfor %}
				</div>
			</div>
		</div>
	</div>


	{% if show_response_form %}
		{% include 'form.html.twig' with {
			'form': form,
			'saveButtonLabel': 'send'|localised
		} only %}
	{% endif %}

{% endblock %}
