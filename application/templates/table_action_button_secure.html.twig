<button data-action="{{ href }}" data-confirmation-question="{{ confirmation }}" data-csrf="{{ csrf }}" class="button-secure bg-nord2 text-white hover:bg-nord3 text-xs font-bold px-3 py-1 rounded outline-none focus:outline-none mr-1 mb-1">{{ name }}</button>
